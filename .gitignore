# In-source CMake build artifacts (this project is built with `cmake .`)
CMakeCache.txt
CMakeFiles/
Makefile
cmake_install.cmake
install_manifest.txt

# Doxygen-Ausgabe, wird per GitHub Action gebaut und auf Pages veroeffentlicht
html/
